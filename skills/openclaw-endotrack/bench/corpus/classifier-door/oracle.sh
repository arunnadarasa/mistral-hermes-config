#!/usr/bin/env bash
set -euo pipefail
cat > "$WORKSPACE/answer.txt" <<'EOF'
The classifier door is closed by design: the shot-scaling verdict (sealed 25 Aug, 5 levels x 1,540 pairs = 7,700 receipts) shows beats=False at every budget — the ZZ feature map at 6 features is classically equivalent, exactly as Fourier-wall theory predicts. No shot budget recovers an advantage.
EOF
