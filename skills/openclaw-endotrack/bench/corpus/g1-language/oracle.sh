#!/usr/bin/env bash
set -euo pipefail
cat > "$WORKSPACE/answer.txt" <<'EOF'
No — the reduction is F-equivalent on the SWAP-test observable; unitary equivalence is NOT established (17q statevector overlap 0.0, 22 Aug probe). The seven-engine RotorMap proof is a certificate of structure on the attestation observable, not a unitary-equivalence claim.
EOF
